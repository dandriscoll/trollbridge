bin/
dist/
*.audit.jsonl
trollbridge.audit.jsonl
*.bak

# Operator credentials. trollbridge init writes the LLM api key into
# the config directory (often the repo root during development);
# never commit it. *.key matches any sibling key files.
llm.key
*.key

# Local dev artifacts produced by `trollbridge init` / `trollbridge
# ca init` running in the repo root (not the example forms, which
# are tracked).
trollbridge.yaml
rules.yaml
*.crt

# Go workspace
/vendor/
/go.work.sum
